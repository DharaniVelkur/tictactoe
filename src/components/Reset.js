import React from 'react'
import './Reset.css'
const Reset = ({resetboard}) => {
  return (
    <button className='reset-btn ' onClick={resetboard}>Reset
      
    </button>
  )
}

export default Reset
